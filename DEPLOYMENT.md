# Foyton 上线配置

## 运行环境

- Node.js 22.5 或更高版本
- 持久化磁盘，用于保存 `data/foyton.db`
- HTTPS 反向代理

## 首次配置

复制 `.env.example` 为 `.env`，并设置以下值：

- `JWT_SECRET`：至少 32 位的随机字符串
- `ENCRYPTION_KEY`：64 个十六进制字符，用于保护上游密钥
- `ADMIN_EMAIL`：首个管理员邮箱
- `ADMIN_PASSWORD`：首个管理员密码
- `SMTP_HOST`、`SMTP_PORT`、`SMTP_SECURE`、`SMTP_USER`、`SMTP_PASSWORD`、`SMTP_FROM`：邮件服务配置

真实密码不得写入 `.env.example`、源代码或提交记录。

## 构建和启动

```bash
npm ci
npm run check
npm run start
```

本地开发使用 `npm run dev`，它会同时启动页面和服务端。注册验证码需要在 `.env` 中填写邮件发送配置后才会发出。

默认服务端口是 `8787`。生产启动会同时提供网页、管理接口、公开状态页和模型调用入口。

## 首次上线顺序

1. 登录 `/admin`。
2. 在“上游管理”中填写供应商地址和密钥，先保持停用。
3. 点击“检查连接”，确认正常后启用。
4. 在“模型与价格”中填写稳定的对外名称、上游真实名称、成本和售价。
5. 用普通用户创建密钥并完成一次小额测试调用。
6. 核对用户余额、调用明细、订单与资金记录、提醒邮件。
7. 在 `/status` 检查公开状态是否与后台一致。

## 备份

定时备份数据库文件，并保留异地副本。备份前应让数据库完成当前写入；恢复流程需要在正式开放前演练一次。
